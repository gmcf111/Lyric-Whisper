# LyricWhisper 运行依赖
#
# 安装方式（统一使用 GPU 版本，CUDA 12.6）：
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu126
#
# GPU 版 torch 自带 CUDA 12.6 + cuDNN 9 运行时 DLL（torch/lib/），
# 无需单独安装 CUDA Toolkit 或下载额外 cuda_libs/ 目录。
# 没有 NVIDIA 显卡的机器也能正常安装和运行（自动降级为 CPU 模式）。

# GUI
PySide6>=6.6.0,<6.12

# 人声分离
demucs>=4.0.0
# torch + torchaudio（CUDA 12.6 版本，自带 cuDNN 9 DLL）
torch>=2.2.0
torchaudio>=2.2.0
# demucs 依赖
julius>=0.2.7
openunmix>=1.2.0
dora-search>=0.1.11
lameenc>=1.2.1
pyyaml>=6.0

# 语音转写（ctranslate2 后端，Windows 包已含 CUDA 支持）
faster-whisper>=1.0.0
ctranslate2>=4.3.0
tokenizers>=0.15.0
onnxruntime>=1.16.0

# 工具
numpy>=1.24.0
soundfile>=0.12.1

# GPU 环境检测（nvidia-ml-py 提供 pynvml 模块，无弃用警告）
nvidia-ml-py>=11.5.0

# 繁简中文转换
zhconv>=1.4.0

# 网络下载
truststore>=0.10.0
httpx>=0.27.0

# 强制对齐（已有歌词文本 → 时间轴对齐）
# 注意：whisperx 默认要求 torch~=2.8.0，与项目 GPU 版 torch 2.12.x+cu126 冲突，
# 必须用 --no-deps 安装 whisperx 本体，再单独装轻量依赖：
#   pip install --no-deps whisperx>=3.4.0
#   pip install transformers>=4.40 nltk>=3.9 pandas>=2.2
# 不要安装 pyannote.audio（仅用于说话人分离，align 用不上，且会大幅增加打包体积）
whisperx>=3.4.0
transformers>=4.40.0
nltk>=3.9.0
pandas>=2.2.0
huggingface_hub>=0.23.0
